import React from 'react';
import TodayOperationDashboard from './today/TodayOperationDashboard';

/** 今日运营一级入口：独立店长每日经营工作台（不接 Courses）。 */
const TodayOperation: React.FC = () => <TodayOperationDashboard />;

export default TodayOperation;
